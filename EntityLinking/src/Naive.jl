# NLP Project - Entity Linking
#
# Naive
#   Contains the naive entity linking algorithm

function naive_linking(tokens, inv_entities, scores, sessions)

end

function naive_link(query, tokens, inv_entities, scores)

end
